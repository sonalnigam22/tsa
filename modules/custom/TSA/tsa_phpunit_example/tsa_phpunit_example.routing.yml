# phpunit_example only has one route. It is to a page explaining
# the module.
tsa_phpunit_example.description:
  path: '/tsa_phpunit_example'
  defaults:
    _controller: '\Drupal\tsa_phpunit_example\Controller\PHPUnitExampleController::description'
  requirements:
    _permission: 'access content'
